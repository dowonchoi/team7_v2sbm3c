<?xml version="1.0" encoding="UTF-8"?>
 
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="dev.mvc.openai.MemberImgDAOInter">
  <insert id="create" parameterType="dev.mvc.openai.MemberImgVO">
      INSERT INTO member_img(member_imgno, memberno, prompt, filename)
      VALUES (member_img_seq.NEXTVAL, #{memberno}, #{prompt}, #{filename})
  </insert>
  
  <select id="list_by_member" parameterType="int" resultType="dev.mvc.openai.MemberImgVO">
      SELECT * FROM member_img WHERE memberno = #{memberno} ORDER BY member_imgno DESC
  </select>
  
  
  
</mapper>