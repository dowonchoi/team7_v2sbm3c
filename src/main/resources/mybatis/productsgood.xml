<?xml version="1.0" encoding="UTF-8"?>
 
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="dev.mvc.productsgood.ProductsgoodDAOInter">
  <!-- 등록 -->
  <insert id="create" parameterType="dev.mvc.productsgood.ProductsgoodVO">
    INSERT INTO productsgood(productsgoodno, rdate, productsno, memberno)
    VALUES (productsgood_seq.nextval, sysdate, #{productsno}, #{memberno})
  </insert>
  
  <!-- 전체 목록 -->
  <select id="list_all" resultType="dev.mvc.productsgood.ProductsgoodVO">
    SELECT productsgoodno, rdate, productsno, memberno
    FROM productsgood
    ORDER BY productsgoodno DESC
  </select>

  <!-- 일반적인 조회 -->
  <select id="read" resultType="dev.mvc.productsgood.ProductsgoodVO" parameterType="int">
    SELECT productsgoodno, rdate, productsno, memberno
    FROM productsgood
    WHERE productsgoodno = #{productsgoodno}
  </select>  
     
  <!-- 삭제 -->
  <delete id="delete" parameterType="int">
    DELETE FROM productsgood
    WHERE productsgoodno = #{productsgoodno}
  </delete>
  
  <!-- 특정 컨텐츠의 특정 회원 추천 갯수 산출, 하나의 글에 대하여 중복 추천 안됨 -->
  <select id="hartCnt" resultType="int" parameterType='HashMap'>
    SELECT COUNT(*) as cnt
    FROM productsgood
    WHERE productsno=#{productsno} AND memberno=#{memberno}
  </select>
  
  <!-- productsno, memberno로 조회 -->
  <select id="readByProductsnoMemberno" resultType="dev.mvc.productsgood.ProductsgoodVO" parameterType="HashMap">
    SELECT productsgoodno, rdate, productsno, memberno
    FROM productsgood
    WHERE productsno=#{productsno} AND memberno=#{memberno}
  </select>  

  <!-- 전체 목록, 테이블 3개 join -->
  <select id="list_all_join" resultType="dev.mvc.productsgood.ProductsproductsgoodMemberVO">
    SELECT r.productsgoodno, r.rdate, r.productsno, c.title as c_title, r.memberno, m.id, m.mname
    FROM products c, productsgood r, member m
    WHERE c.productsno = r.productsno AND r.memberno = m.memberno
    ORDER BY productsgoodno DESC
  </select>

  <!-- productsno + memberno 조합으로 추천 삭제 -->
  <delete id="deleteByProductsnoMemberno" parameterType="HashMap">
    DELETE FROM productsgood
    WHERE productsno = #{productsno} AND memberno = #{memberno}
  </delete>

  <!-- 소비자: 내가 하트 누른 상품 리스트 -->
  <select id="list_user_liked_products" resultType="dev.mvc.products.ProductsVO" parameterType="int">
    SELECT p.*
    FROM products p
    JOIN productsgood g ON p.productsno = g.productsno
    WHERE g.memberno = #{memberno}
    ORDER BY g.productsgoodno DESC
  </select>

  <!-- 공급자: 내가 등록한 상품 중 추천 받은 상품 리스트 -->
  <select id="list_supplier_products_liked" resultType="dev.mvc.products.ProductsVO" parameterType="int">
    SELECT *
    FROM products
    WHERE memberno = #{memberno}
      AND recom > 0
    ORDER BY recom DESC
  </select>
  
  <select id="getProductsgoodByMember" resultType="dev.mvc.products.ProductsVO">
    SELECT P.*
    FROM productsgood G
      JOIN products P ON G.productsno = P.productsno
    WHERE G.memberno = #{memberno}
    ORDER BY G.productsno DESC
  </select>

</mapper>
